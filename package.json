{
  "name": "student-affairs-system",
  "version": "1.0.0",
  "description": "Student Affairs System",
  "scripts": {
    "start": "json-server db.json --port $PORT --host 0.0.0.0 --static ./public"
  },
  "dependencies": {
    "json-server": "^0.17.4",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": "18.x"
  }
}